<%- include('./partials/head.ejs') %>
<%- include('./partials/header.ejs') %>

<main>
  <section>
    <% Pets.forEach(pet=>{%>
      <h1><%=pet.name  %></h1>
      <h2>Owner: <%= pet.owner %></h2>
        <h3>Vet:<%= pet.vet %></h3>
        <p>Species: <%= pet.species %></p>
  <%  }) %>
  </section>
</main>

</body>
</html>